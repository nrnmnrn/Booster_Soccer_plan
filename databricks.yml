# This is a Databricks asset bundle definition for Booster_Soccer_plan.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Booster_Soccer_plan
  uuid: 519185da-852d-4666-b15a-2f17dc88dbe3

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://2748439443229302.2.gcp.databricks.com
    variables:
      catalog: poc_test
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://2748439443229302.2.gcp.databricks.com
      # We explicitly deploy to /Workspace/Users/adamlin@cheerstech.com.tw to make sure we only have a single copy.
      root_path: /Workspace/Users/adamlin@cheerstech.com.tw/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: poc_test
      schema: prod
    permissions:
      - user_name: adamlin@cheerstech.com.tw
        level: CAN_MANAGE
